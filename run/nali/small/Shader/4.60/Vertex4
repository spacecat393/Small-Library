#version 460 core

in vec4 vertices;

uniform mat4 ModelViewMatrix;
uniform mat4 ProjectionMatrix;

out vec2 fragment_texcoord;

void main()
{
    gl_Position = ProjectionMatrix * ModelViewMatrix * vec4(vertices.xy, 0.0, 1.0);
    fragment_texcoord = vertices.zw;
}
